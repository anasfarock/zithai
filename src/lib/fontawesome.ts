import { config } from '@fortawesome/fontawesome-svg-core'

// Tell FontAwesome to skip adding the CSS automatically 
// since it's being imported in the layout
config.autoAddCss = false

export default config